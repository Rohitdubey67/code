#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    int a,b,x;
    cin>>t;
    while(t--)
    {
        cin>>a>>b;
        x=3*a-4*b;
        if(x<0)
        cout<<0<<endl;
        else
        cout<<x<<endl;
    }
}
